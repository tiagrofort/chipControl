<UserControl x:Class="ChipControl.Presentation.WPF.Views.DashboardView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:vm="clr-namespace:ChipControl.Presentation.WPF.ViewModels">
    <UserControl.DataContext>
        <vm:MainViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0">
            <StackPanel Margin="24">
                <TextBlock Text="Dashboard"
                           FontFamily="Work Sans"
                           FontSize="20" FontWeight="SemiBold"
                           Foreground="#004277" Margin="0,0,0,24"/>
                <Border Background="#FFFFFF" BorderBrush="#C1C7D2" BorderThickness="1"
                        CornerRadius="8" Padding="24" Margin="0,0,0,16">
                    <TextBlock TextWrapping="Wrap"
                               FontFamily="Work Sans"
                               FontSize="13"
                               Foreground="#414750">
                        <Run FontWeight="SemiBold">Dashboard</Run> — placeholder para indicadores futuros.
                    </TextBlock>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
